{
  "name": "quick-link-popup",
  "version": "2.0.0",
  "description": "Text selection popup: internal links, path copy, Cosense note creation, paragraph splitting",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --external:obsidian --outfile=main.js --platform=browser --format=cjs --watch",
    "build": "esbuild src/main.ts --bundle --external:obsidian --outfile=main.js --platform=browser --format=cjs",
    "test": "jest --verbose",
    "test:coverage": "jest --verbose --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "obsidian": "latest"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^16.18.126",
    "esbuild": "^0.18.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.0.0"
  }
}
